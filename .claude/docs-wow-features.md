# Docs "wow"-feature research (background agent, 2026-06-14)
See chat for the full numbered list. Top 5 wow-per-effort:
1. Animated Plotly frames (play/slider over passes) — animate the loop converging.
2. Closeread scrollytelling on the Tutorial (pin a Fisher/Bode fig, reveal terms).
3. Examples as a Listing card gallery + Lightbox.
4. ojs reactive sliders (drag Q/f0, watch the peak move) — static, no server.
5. Social cards (set site-url/open-graph) + numbered code annotations.
Runner-up: quarto-live Pyodide "Try it live" cell (validate sysid imports under WASM).
Server-only (AVOID on Pages): Shiny.
